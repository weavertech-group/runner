name: Lark session card
description: Create and update the private runner session card.

inputs:
  status:
    description: Session status to publish.
    required: true

runs:
  using: node24
  main: index.js
  post: cleanup.js
